spring.application.name=academic-management-service

spring.datasource.url=jdbc:mysql://localhost:3306/Academic_Management?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

server.port=8086

eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.hostname=localhost

spring.cloud.config.enabled=false

server.error.include-message=always
server.error.include-stacktrace=always

server.servlet.context-path=/academics/api

cloudinary.cloud_name=ddtfi7kal
cloudinary.api_key=971417699379591
cloudinary.api_secret=SzcF1Ll17RR2oMvueIXikFAN2IM

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chaibimarwa18@gmail.com
spring.mail.password=njtkanipwsvtsuoy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.debug=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

logging.level.feign=DEBUG
logging.level.org.springframework.cloud.openfeign=DEBUG
logging.level.com.netflix.discovery=DEBUG
logging.level.com.netflix.loadbalancer=DEBUG
logging.level.org.springframework.cloud.loadbalancer=DEBUG

# ============================================================================
# KEYCLOAK / OAuth2 Resource Server Configuration
# ============================================================================
# Issuer URI pour la validation des JWT Keycloak
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:9090/realms/devunity}

# Configuration Keycloak (pour référence)
keycloak.realm=${KEYCLOAK_REALM:devunity}
keycloak.auth-server-url=${KEYCLOAK_URL:http://localhost:9090}
keycloak.resource=${KEYCLOAK_CLIENT_ID:devunity-app}

management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.prometheus.enabled=true
management.prometheus.metrics.export.enabled=true
apiVersion: v1
kind: Service
metadata:
  name: learner-management-service
  namespace: devunity
spec:
  selector:
    app: learner-management-service
  type: NodePort
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
stringData:
  # Valeurs injectees via --set dans le Jenkinsfile_CD (ne jamais hardcoder ici)
  mysql-root-password: "${MYSQL_ROOT_PASSWORD}"
  mysql-database: "Language_Courses"
  mysql-username: "${MYSQL_USERNAME}"

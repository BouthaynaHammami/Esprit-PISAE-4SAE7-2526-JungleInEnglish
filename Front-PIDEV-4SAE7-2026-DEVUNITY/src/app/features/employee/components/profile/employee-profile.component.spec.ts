import { EmployeeProfileComponent } from './employee-profile.component';

describe('EmployeeProfileComponent', () => {
  it('should be defined', () => {
    expect(EmployeeProfileComponent).toBeDefined();
  });
});


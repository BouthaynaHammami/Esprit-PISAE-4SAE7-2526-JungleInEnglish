import { StudentQuizComponent } from './student-quiz.component';

describe('StudentQuizComponent', () => {
  it('should be defined', () => {
    expect(StudentQuizComponent).toBeDefined();
  });
});


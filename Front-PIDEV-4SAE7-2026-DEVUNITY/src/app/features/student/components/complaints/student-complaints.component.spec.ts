import { StudentComplaintsComponent } from './student-complaints.component';

describe('StudentComplaintsComponent', () => {
  it('should be defined', () => {
    expect(StudentComplaintsComponent).toBeDefined();
  });
});


import { StudentNavbarComponent } from './student-navbar.component';

describe('StudentNavbarComponent', () => {
  it('should be defined', () => {
    expect(StudentNavbarComponent).toBeDefined();
  });
});


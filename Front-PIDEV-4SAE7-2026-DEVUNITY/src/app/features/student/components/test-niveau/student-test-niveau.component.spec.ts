import { StudentTestNiveauComponent } from './student-test-niveau.component';

describe('StudentTestNiveauComponent', () => {
  it('should be defined', () => {
    expect(StudentTestNiveauComponent).toBeDefined();
  });
});


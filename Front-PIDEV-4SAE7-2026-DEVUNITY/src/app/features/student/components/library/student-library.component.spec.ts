import { StudentLibraryComponent } from './student-library.component';

describe('StudentLibraryComponent', () => {
  it('should be defined', () => {
    expect(StudentLibraryComponent).toBeDefined();
  });
});


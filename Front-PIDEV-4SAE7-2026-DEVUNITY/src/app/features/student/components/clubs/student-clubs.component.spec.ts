import { StudentClubsComponent } from './student-clubs.component';

describe('StudentClubsComponent', () => {
  it('should be defined', () => {
    expect(StudentClubsComponent).toBeDefined();
  });
});


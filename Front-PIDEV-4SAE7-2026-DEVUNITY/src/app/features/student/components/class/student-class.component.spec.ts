import { StudentClassComponent } from './student-class.component';

describe('StudentClassComponent', () => {
  it('should be defined', () => {
    expect(StudentClassComponent).toBeDefined();
  });
});


import { StudentDashboardComponent } from './student-dashboard.component';

describe('StudentDashboardComponent', () => {
  it('should be defined', () => {
    expect(StudentDashboardComponent).toBeDefined();
  });
});


import { ScheduleListComponent } from './schedule-list.component';

describe('ScheduleListComponent', () => {
  it('should be defined', () => {
    expect(ScheduleListComponent).toBeDefined();
  });
});


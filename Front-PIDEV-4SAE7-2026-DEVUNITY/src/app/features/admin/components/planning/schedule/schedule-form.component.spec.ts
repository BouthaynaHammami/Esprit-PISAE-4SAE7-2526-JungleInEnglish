import { ScheduleFormComponent } from './schedule-form.component';

describe('ScheduleFormComponent', () => {
  it('should be defined', () => {
    expect(ScheduleFormComponent).toBeDefined();
  });
});


import { ClassDetailComponent } from './class-detail.component';

describe('ClassDetailComponent', () => {
  it('should be defined', () => {
    expect(ClassDetailComponent).toBeDefined();
  });
});


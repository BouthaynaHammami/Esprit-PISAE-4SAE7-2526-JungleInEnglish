import { ComplaintDetailComponent } from './complaint-detail.component';

describe('ComplaintDetailComponent', () => {
  it('should be defined', () => {
    expect(ComplaintDetailComponent).toBeDefined();
  });
});


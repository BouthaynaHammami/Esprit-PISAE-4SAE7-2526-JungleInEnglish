import { ComplaintListComponent } from './complaint-list.component';

describe('ComplaintListComponent', () => {
  it('should be defined', () => {
    expect(ComplaintListComponent).toBeDefined();
  });
});


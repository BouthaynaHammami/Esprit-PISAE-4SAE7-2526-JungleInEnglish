import { AdminQuizzesComponent } from './admin-quizzes.component';

describe('AdminQuizzesComponent', () => {
  it('should be defined', () => {
    expect(AdminQuizzesComponent).toBeDefined();
  });
});


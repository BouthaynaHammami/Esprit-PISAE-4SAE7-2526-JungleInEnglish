import { AdminTestNiveauComponent } from './admin-test-niveau.component';

describe('AdminTestNiveauComponent', () => {
  it('should be defined', () => {
    expect(AdminTestNiveauComponent).toBeDefined();
  });
});


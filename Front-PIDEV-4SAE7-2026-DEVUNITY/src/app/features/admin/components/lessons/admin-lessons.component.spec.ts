import { AdminLessonsComponent } from './admin-lessons.component';

describe('AdminLessonsComponent', () => {
  it('should be defined', () => {
    expect(AdminLessonsComponent).toBeDefined();
  });
});


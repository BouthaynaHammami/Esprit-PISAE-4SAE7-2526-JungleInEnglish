import { AdminSidebarComponent } from './admin-sidebar.component';

describe('AdminSidebarComponent', () => {
  it('should be defined', () => {
    expect(AdminSidebarComponent).toBeDefined();
  });
});

